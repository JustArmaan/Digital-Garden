@model MVCView.Models.CommunityTip

<div id="details-container" class="bg-light p-4 rounded">
    <h4 class="text-success text-center fw-bold">Tip Details</h4>

    <div class="card">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0">💧 @Model.Title</h5>
        </div>
        <div class="card-body">
            <p class="card-text">@Model.Content</p>
            <div class="d-flex justify-content-between">
                <small class="text-muted">Submitted by: @Model.SubmittedBy</small>
                <small class="text-muted">Date: @Model.SubmittedDate.ToString("MMM dd, yyyy")</small>
            </div>
        </div>
    </div>

    <button type="button" id="back-to-list" class="btn btn-secondary w-100 mt-3">Back to List</button>
</div>
