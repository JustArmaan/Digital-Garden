<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Plant - Digital Garden</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            max-width: 600px;
            margin: auto;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .form-control, .form-select {
            border-radius: 8px;
        }
        .btn-success {
            width: 100%;
            border-radius: 8px;
            transition: 0.3s ease-in-out;
        }
        .btn-success:hover {
            background-color: #28a745;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="container mt-5 mb-5">
        <div class="card p-4">
            <h2 class="text-center text-success"><i class="fas fa-seedling"></i> Add a New Plant</h2>
            <p class="text-center text-muted">Fill in the details below to add a new plant to your collection.</p>

            <form action="submit_plant.php" method="POST" class="mt-3">
                <div class="mb-3">
                    <label for="plantName" class="form-label"><i class="fas fa-leaf"></i> Plant Name</label>
                    <input type="text" class="form-control" id="plantName" name="plantName" placeholder="Enter plant name" required>
                </div>

                <div class="mb-3">
                    <label for="species" class="form-label"><i class="fas fa-tree"></i> Species</label>
                    <input type="text" class="form-control" id="species" name="species" placeholder="Enter species">
                </div>

                <div class="mb-3">
                    <label for="wateringSchedule" class="form-label"><i class="fas fa-tint"></i> Watering Schedule (days)</label>
                    <input type="number" class="form-control" id="wateringSchedule" name="wateringSchedule" min="1" required>
                </div>

                <div class="mb-3">
                    <label for="sunlight" class="form-label"><i class="fas fa-sun"></i> Sunlight Requirements</label>
                    <select class="form-select" id="sunlight" name="sunlight">
                        <option value="Full Sun">Full Sun</option>
                        <option value="Partial Shade">Partial Shade</option>
                        <option value="Low Light">Low Light</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="notes" class="form-label"><i class="fas fa-sticky-note"></i> Additional Notes</label>
                    <textarea class="form-control" id="notes" name="notes" rows="3" placeholder="Optional notes..."></textarea>
                </div>

                <button type="submit" class="btn btn-success"><i class="fas fa-plus"></i> Add Plant</button>
            </form>
        </div>
    </div>
</body>
</html>
